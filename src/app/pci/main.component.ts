import { Component } from '@angular/core';

@Component({
  selector: 'app-pci',
  template: `
    <app-nav>
      <router-outlet></router-outlet>
    </app-nav>
  `
})
export class PciMainComponent {
}
